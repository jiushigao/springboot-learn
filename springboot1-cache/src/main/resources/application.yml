spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_cache?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    host: 192.168.1.122
mybatis:
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    com.example.cache.mapper: debug

